#Comandos de la App
```Options:
      --help           Show help                  [boolean]
      --version        Show version number        [boolean]
  -t, --tipoOperation  the aritmetic operation
                         [string] [required] [default: "S"]
  -b, --base           the aritmetic base
                           [number] [required] [default: 5]
  -l, --limit          the aritmetic base
                          [number] [required] [default: 10]
  -s, --list           the aritmetic base
                                 [boolean] [default: false]
```